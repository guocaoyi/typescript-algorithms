/**
 * 笛卡尔积（一维数组）
 * @param arr [number[], number[]]
 */
export const cartesian = (arr: [number[], number[]]) => {
  //
};

/**
 * 笛卡尔积（多阶）
 * @param arr number[][]
 */
export const double_cartesian = (arr: number[][]) => {
  //
};
